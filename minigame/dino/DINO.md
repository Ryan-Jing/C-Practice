# Dino Terminal Sidescroll Game
First time using termios.h. It is a built-in library as part of the Portable OS System Interface
(POSIX). It allows me to control terminal behaviour, so that I can easily interpret user keyboard
input.